#!/bin/bash

files=(~/pix/wallpaper/*)
index=$(seq 0 $((${#files[@]}-1)) | shuf -n1)
wall="${files[$index]}"

swww img $wall --transition-type grow --transition-pos "$(cursorpos-inv)" --transition-duration 2

